baseURL = 'https://hrmnjt.dev'
languageCode = 'en-us'
title = 'hrmnjt.dev::sttp:://secure_thought_transfer_protocol'

[permalinks]
  shards = '/:year/:month/:day/:filename/'

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    codeFences = true
    guessSyntax = false
    lineNos = false
    noClasses = false
    tabWidth = 2

# [params]
#   description = 'A blog of rough drafts and unpolished thoughts'
#   author = 'Your Name'

# Disable taxonomy pages
disableKinds = ['taxonomy', 'term']
